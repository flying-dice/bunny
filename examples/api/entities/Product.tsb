import type { ProductId } from "../types/ProductId.ts";

#[derive(Clone, Equals, ToJson)]
struct Product {
  id: ProductId,
  #[minLength(1), maxLength(200)]
  name: string,
  #[minimum(0)]
  priceCents: number,
  #[minimum(0)]
  stock: number,
}
