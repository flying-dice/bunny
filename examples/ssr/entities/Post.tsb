#[derive(Clone, ToJson)]
struct Post {
  #[format("uuid")]
  id: string,
  #[minLength(1), maxLength(200)]
  title: string,
  #[minLength(1)]
  body: string,
  #[format("date-time")]
  publishedAt: string,
}
